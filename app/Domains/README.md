- Để tạo thư mục trong domains sử dụng lệnh:
    + php artisan crud:frame {name : quy tắc đặt tên giống Model User} 
        {--relationship : option này giúp sinh ra floder dùng để chứa các quan hệ của Model}
        {--enum : option này giúp sinh ra floder dùng để chứa các constant của Model}
